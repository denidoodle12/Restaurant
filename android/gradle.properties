org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable Kotlin incremental compilation — pub cache is on a different drive
# than the project, which breaks relocatable file paths in incremental caches.
kotlin.incremental=false
# Disable parallel & daemon caching to reduce memory pressure on low-RAM systems.
org.gradle.parallel=false
org.gradle.caching=false
